export interface IState{
    details : {
        id : string,
        userId : string,
        title : string,
        body : string
    }
} 

export interface ParamTypes {
    id: string;
    userId : string;
}
